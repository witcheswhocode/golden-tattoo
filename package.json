{
  "name": "golden-tattoo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "ts-node ./server/server.mjs",
    "heroku-postbuild": "cd frontend && yarn install && yarn build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "sqlite3": "^5.1.7"
  },
  "engines": {
    "node": "16.x",
    "yarn": "1.22.x"  
  }
}
